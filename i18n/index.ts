import { pt } from './pt';
import { es } from './es';
import { en } from './en';

export type Language = 'pt' | 'es' | 'en';

export const translations = {
  pt,
  es,
  en,
};

export { pt, es, en };
